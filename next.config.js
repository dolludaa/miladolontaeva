/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/DolontaevaanalysisWebCite',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig 